html
    head
        title User Manager
    body
        h1 Users
        form(action="/table", method="GET", id="search")
            input(type='text', placeholder='Search by Name')
            button(type='submit') Submit
        label(for='sort') Sort Alphebetically:
        select(name="sort" id="sort")
            option(value='A-Z') A-Z
            option(value='Z-A') Z-A
        table(style='width:100%', border='1')
            tr
                th User ID
                th First Name
                th Last Name
                th Email
                th Age
            each item in users
                tr
                    td= item._id
                    td= item.firstName
                    td= item.lastName
                    td= item.email
                    td= item.age
                    td
                        a(href=`/edit/${item._id}`, name='edit') Edit
                    td
                        form(method="post", action="/table")
                            input(type="hidden", id="userId", name="userId" value=`${item._id}`)
                            button(name='delete', id="delete") Delete